[tool.poetry]
name = "shop-electro"
version = "0.1.0"
description = "Shop_electro"
authors = ["Natalia"]
readme = "README.md"
packages = [{include = "\\"}]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
